package com.g4.tp.model.strategy;
/* 
public class ContextStrategy {

    IMatchingStrategy strategy;

    public ContextStrategy(IMatchingStrategy strategy) {
        this.strategy = strategy;
    }
    public IMatchingStrategy getStrategy() {
        return strategy;
    }
    public void setStrategy(IMatchingStrategy strategy) {
        this.strategy = strategy;
    }
    public void executeStrategy() {
        if (strategy != null) {
            strategy.matchPlayers();
        } else {
            throw new IllegalStateException("Matching strategy is not set.");
        }
    }
    public void executeStrategyWithPlayers(List<User> availableUsers, Match match) {
        if (strategy != null) {
            strategy.matchPlayers(availableUsers, match);
        } else {
            throw new IllegalStateException("Matching strategy is not set.");
        }
    }

}
*/